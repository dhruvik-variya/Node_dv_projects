<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #f4d03f, #16a085);
            margin: 0;
        }

        h1 {
            text-align: center;
            background-color: #2c3e50;
            color: #ecf0f1;
            padding: 20px 40px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        table {
            margin-top: 50px;
            border-collapse: separate;
            border-spacing: 0;
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
            padding: 20px;
        }

        td {
            padding: 15px;
        }

        label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
            color: #34495e;
        }

        input {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 8px;
            width: 250px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        input:focus {
            border-color: #16a085;
            outline: none;
            box-shadow: 0 0 5px rgba(22, 160, 133, 0.5);
        }

        input[type="submit"] {
            background-color: #16a085;
            color: white;
            border: none;
            cursor: pointer;
            width: 100%;
            transition: background 0.3s ease;
            font-weight: bold;
        }

        input[type="submit"]:hover {
            background-color: #138d75;
        }
    </style>
</head>

<body>
    <h1>This is Edit page</h1>
    <form action="/edituser" method="post">
        <table>
            <tr>
                <td>
                    <label for="">Id :</label>
                    <input type="number" name="editid" value="<%= student.userid %>" placeholder="Enter userid" id="" readonly>
                </td>
            </tr>
            <tr>
                <td>
                    <label for="">Name :</label>
                    <input type="text" name="name" value="<%= student.name %>" placeholder="Enter username" id="">
                </td>
            </tr>
            <tr>
                <td>
                    <label for="">Email :</label>
                    <input type="email" name="email" value="<%= student.email %>" placeholder="Enter useremail" id="">
                </td>
            </tr>
            <tr>
                <td>
                    <label for="">Password :</label>
                    <input type="password" name="password" value="<%= student.password %>" placeholder="Enter userpassword" id="">
                </td>
            </tr>
            <tr>
                <td>
                    <input type="submit" value="Update">
                </td>
            </tr>
        </table>
    </form>
</body>

</html>
